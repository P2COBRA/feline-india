{
  "name": "server",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npx prisma db push --schema=./prisma/schema.prisma && node --watch index.js",
    "start": "node index.js",
    "seed": "npx prisma db push --schema=./prisma/schema.prisma && node seed.js",
    "postinstall": "npx prisma generate --schema=./prisma/schema.prisma && npx prisma db push --schema=./prisma/schema.prisma"
  },
  "dependencies": {
    "@prisma/client": "^5.20.1",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.1",
    "express": "^4.21.2",
    "express-session": "^1.18.0",
    "multer": "^2.0.0",
    "prisma": "^5.20.1"
  }
}
